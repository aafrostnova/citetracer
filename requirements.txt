# =============================================================================
# Core runtime — required for every install (Option A and Option B both need
# these). All pure-Python or platform-independent; safe to install with a
# plain `pip install -r requirements.txt` regardless of CUDA / GPU choice.
# =============================================================================
pypdf>=4.0.0
pymupdf>=1.24.0
boto3>=1.34.0
requests>=2.32.0
Pillow>=10.0.0
einops
addict
easydict
safetensors>=0.4.0

# =============================================================================
# torch / torchvision / transformers / tokenizers / vllm / flash-attn are
# DELIBERATELY NOT pinned here:
#
#   - Option A (all-Bedrock, no GPU) does not need any of them.
#   - Option B (local DeepSeek-OCR-2) installs them in the order the
#     upstream repo specifies (cu118 wheels for torch, vllm 0.8.5 cu118
#     wheel, then DeepSeek-OCR-2/requirements.txt for transformers, then
#     flash-attn with --no-build-isolation). Pinning them here would
#     either force unnecessary CUDA downloads on Option A or fight with
#     the CUDA-matched wheels on Option B.
#
# See README §1 Option B for the exact install commands.
# =============================================================================
